import { PageScreen } from "../../components/ui/PageScreen";

export function NotificationsPage() {
  return <PageScreen title="Notifications" subtitle="All alerts, updates, and direct workflow links." />;
}
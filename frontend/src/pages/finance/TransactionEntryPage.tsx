import { PageScreen } from "../../components/ui/PageScreen";

export function TransactionEntryPage() {
  return <PageScreen title="New Transaction" subtitle="Add income or expenditure to the ledger." />;
}